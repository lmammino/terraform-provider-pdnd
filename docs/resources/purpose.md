---
page_title: "pdnd_purpose Resource - PDND"
description: |-
  Manages a PDND Purpose lifecycle.
---

# pdnd_purpose (Resource)

Manages a PDND Purpose lifecycle, including creation, activation, suspension, and quota updates.

The `desired_state` attribute controls the intended lifecycle state. The provider will automatically perform the necessary API operations (activate, suspend, unsuspend) to reach the desired state.

When `daily_calls` exceeds the e-service threshold, activation may result in a `WAITING_FOR_APPROVAL` state. Set `allow_waiting_for_approval = true` to allow this.

## Example Usage

```terraform
# Create a purpose in DRAFT state
resource "pdnd_purpose" "draft" {
  eservice_id       = pdnd_eservice.example.id
  title             = "My Purpose"
  description       = "Access to example e-service"
  daily_calls       = 1000
  is_free_of_charge = false
  desired_state     = "DRAFT"
}

# Create a purpose and activate it
resource "pdnd_purpose" "active" {
  eservice_id       = pdnd_eservice.example.id
  title             = "My Active Purpose"
  description       = "Active access to example e-service"
  daily_calls       = 5000
  is_free_of_charge = true
  desired_state     = "ACTIVE"
}

# Create a purpose with high daily calls that may need approval
resource "pdnd_purpose" "high_calls" {
  eservice_id                = pdnd_eservice.example.id
  title                      = "High Volume Purpose"
  description                = "High volume access to example e-service"
  daily_calls                = 100000
  is_free_of_charge          = false
  desired_state              = "ACTIVE"
  allow_waiting_for_approval = true
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `daily_calls` (Number) Maximum daily API calls (1-1,000,000,000)
- `description` (String) Purpose description (10-250 characters)
- `desired_state` (String) Desired purpose state: DRAFT, ACTIVE, or SUSPENDED
- `eservice_id` (String) E-Service UUID
- `is_free_of_charge` (Boolean) Whether the service is free of charge
- `title` (String) Purpose title (5-60 characters)

### Optional

- `allow_waiting_for_approval` (Boolean) If true, allow the purpose to remain in WAITING_FOR_APPROVAL state after activation
- `delegation_id` (String) Delegation UUID for delegated consumers
- `free_of_charge_reason` (String) Reason for free of charge, if applicable

### Read-Only

- `consumer_id` (String) Consumer tenant UUID
- `created_at` (String) Creation timestamp
- `first_activation_at` (String) First activation timestamp
- `id` (String) Purpose UUID
- `is_risk_analysis_valid` (Boolean) Whether the risk analysis is valid
- `rejection_reason` (String) Rejection reason, if rejected
- `state` (String) Observed server state of the purpose
- `suspended_at` (String) Suspension timestamp
- `suspended_by_consumer` (Boolean) Whether suspended by consumer
- `suspended_by_producer` (Boolean) Whether suspended by producer
- `updated_at` (String) Last update timestamp
- `version_id` (String) Current version UUID

## Import

Purposes can be imported using their UUID:

```shell
terraform import pdnd_purpose.example 990e8400-e29b-41d4-a716-446655440000
```
